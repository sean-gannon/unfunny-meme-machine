name: CI Build

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Frontend Build
        run: |
          cd frontend
          npm ci
          npm run build

      - name: Backend Build
        run: |
          cd backend
          ./mvnw clean verify
